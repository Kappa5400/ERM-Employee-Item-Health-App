1. Security (biggest red flag — do first)

x Lock H2 console to test profile only, remove from SecurityConfig prod config
x Fix CSRF — re-enable or add a note explaining why it's disabled
Fix deploy.yml secrets — move to --env-file or .env approach
Replace employeeId == 1 magic number with a constan tor isSuperAdmin() flag

2. Bug fixes (broken functionality)

Fix ID card forms
Fix expire/renew logic and verify it's correct
After item updates, set all to_renew/to_replace/to_service bools back to false in SQL
Fix login error handling — add alerts, validate required fields like email
Fix mail body — show employee name, item type, item ID together properly

3. Input validation

Add server-side validation on all forms (employee create/edit, items)
Add frontend alerts for missing/invalid fields
Handle edge cases — employee with no boss, items with no employee, etc.

4. Manual retest of full site

Walk every page and form end to end
Boss dashboard, employee pages, all item CRUD
Email flow triggered correctly
Login/logout, auth guards working

5. Automated testing

Unit tests for health check + email flow (the core feature)
Fix/complete existing test files
Add CI/CD test step — mvn test in deploy.yml before deploy
Verify README claim about "faulty code never deployed" matches reality


7. Documentation

Clean README with setup instructions, how to run, architecture overview


8. Later / nice to have

Replace magic big boss reassignment with proper admin role
